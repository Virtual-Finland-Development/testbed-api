[package]
name = "http_server"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["rlib", "dylib"]
path = "src/http_server.rs"

[dependencies]
hyper = { version = "0.14.12", optional = false }
log = "0.4.14"
lambda_http = "0.7.0"
api_app = { path = "../api_app" }
