[package]
name = "lambda_service"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["rlib", "dylib"]
path = "src/lambda_service.rs"

[dependencies]
log = "0.4.14"
lambda_http = "0.7.0"
api_app = { path = "../api_app" }
