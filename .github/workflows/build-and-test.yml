name: Build and test

on:
  pull_request:
    branches:
      - main
  workflow_call:
    inputs:
      deployment_stage:
        required: true
        type: string
      wants_artifacts:
        required: false
        type: boolean
        default: false
jobs:
  build-and-test:
    name: Build and test ⌛
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build 🔧
        run: |
          echo $TEST_ENV_VARIABLE > moro.txt
          cat moro.txt
          make build
        env:
          TEST_ENV_VARIABLE: ${{ secrets.TEST_ENV_VARIABLE }}
      - name: Test 🧪
        run: |
          make test
      - name: Archive the build artifacts 🐪
        if: ${{ inputs.wants_artifacts }}
        uses: actions/upload-artifact@v3
        with:
          name: build-artifact-${{ inputs.deployment_stage }}
          path: infra/build/rust.zip
          if-no-files-found: error
